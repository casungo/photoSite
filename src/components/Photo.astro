---
const { img, imgThumb, alt, width, height } = Astro.props;
---
    <a
      href={img}
      data-pswp-width={width}
      data-pswp-height={height}
      target="_blank"
    >
      <img
        src={img}
        alt={alt}
        class="img-fluid"
        width="25%"
      />
    </a>